# 本地图字体放置说明

将以下字体文件复制到本目录（`frontend/public/fonts`），保持文件名一致即可：

- `Geist-Regular.ttf`（正文字体 Regular/400）
- `Geist-SemiBold.ttf`（正文字体 SemiBold/600）
- `GeistMono-Regular.ttf`（等宽字体 Regular/400）

如果你有更多字重或其它字体文件，可以在 `frontend/app/globals.css` 中追加对应的 `@font-face` 声明或调整文件名。
